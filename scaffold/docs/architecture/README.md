# Architecture

Module boundaries and dependency direction live here. Create `boundaries.md` (and other files) when the first real boundary decision lands — do not invent empty structure ahead of content. See `docs/memory.md`.
